//write the program to print the value of variable.
#include <stdio.h>

int main()

{

    // initialize the variable

    int n = 3;

    // display the value of the variable

    printf("the value of the variable is %d", n);

    return 0;
}